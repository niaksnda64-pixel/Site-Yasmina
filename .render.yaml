services:
  - type: web
    name: site-yasmina
    env: node
    rootDir: backend
    buildCommand: npm install --build-from-source
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
